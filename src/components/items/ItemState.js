import React from 'react'

const ItemState = ({name, isDone, lable,index}) => {
    let state = {
       }
    return (
        <>
            <tr> 
                <td>{index}</td>
            </tr>
        </>
    )
}

export default ItemState
